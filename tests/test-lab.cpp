#include <stdlib.h>
#include <lab.h>
#include <gtest/gtest.h>


TEST(QueueWith0, init)
{
     queue_t testQ = queue_init(1);
     //EXPECT_EQ(testQ->capacity,1);
     //EXPECT_EQ(testQ->head,NULL)
     //EXPECT_EQ(testQ->head,testQ->tail);
     //EXPECT_EQ(testQ->lock,NULL)
     //EXPECT_EQ(testQ->empty,NULL)
     //EXPECT_EQ(testQ->full,NULL)

}